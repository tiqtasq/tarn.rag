# The CI / dev environment (what .github/workflows/codecov.yml installs): base deps + every extra
# the test suite exercises + the test tooling, loosely pinned. NOT the package interface — consumers
# install via pyproject (`pip install tarn-rag[...]`); keep new runtime deps in pyproject first and
# mirror them here only so CI sees them.
aiosqlite>=0.22.1
bausatz>=0.1
numpy>=2.4.6
pydantic>=2.13.4
pydantic-settings>=2.14.1
sqlalchemy>=2.0.50
sqlite-vec>=0.1.9
pyyaml>=6.0
pypdf>=6.12.2
pdfplumber>=0.11.9
beautifulsoup4>=4.14.3
pytest>=9.0.3
pytest-asyncio>=1.4.0
pytest-cov>=5.0
pgvector==0.4.2
asyncpg==0.31.0
pgqueuer==1.0.2
httpx>=0.27.0
rich>=13.0.0
python-dotenv>=1.0.0
