"""Part II — the failure-driven complexity ladder (see ``PLAN.md`` / ``CORPUS.md``).

Each example is a YAML config runnable with ``python -m tarnrag.console cfg.yaml``; its ``run.py`` uses the
shared :mod:`examples.part_ii._runner` to narrate the good-vs-bad probes around that config.
"""
